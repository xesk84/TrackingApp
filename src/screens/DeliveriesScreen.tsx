import React from 'react';
import {View, Text, SafeAreaView} from 'react-native';

type Props = {};

export const DeliveriesScreen = (props: Props) => {
  return (
    <SafeAreaView>
      <Text>Deliveries</Text>
    </SafeAreaView>
  );
};
